// JavaScript Document

//global variables for collectables

var position = "START";	//Declaration for our position varaible. Starting at START

var hasRib = false;			//If the user has collected the RIB
var hasBelt = false;		//If the user has collected the BELT
var hasGauntlet = false;	//If the user has collected the GAUNTLET
var hasKey = false;			//If the user has collected the KEY
var doorOpen = false;		//If the user has opened the CELLDOOR